# Example environment file for docker-compose.yaml
# Copy to `.env` and adjust before running `docker compose up -d`.

# API key that Qdrant will require when clients connect to localhost:6333
QDRANT_API_KEY=change-me
